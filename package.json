{
  "name": "root",
  "private": true,
  "devDependencies": {
    "eslint": "^7.2.0",
    "http-server": "^0.12.3",
    "lerna": "^3.18.4",
    "prettier": "^2.1.2"
  },
  "scripts": {
    "lint": "lerna exec --parallel npm run lint",
    "format": "lerna exec --parallel npm run format",
    "preversion": "git pull",
    "releaseMinor": "lerna version minor",
    "lernaInstall": "lerna exec --parallel npm install"
  },
  "dependencies": {
    "DataStream.js": "github:kig/DataStream.js",
    "jquery": "^1.11.1",
    "three": "^0.91.0",
    "w2ui": "^1.4.3"
  }
}
