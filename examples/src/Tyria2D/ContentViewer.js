//Content viewer is a seprate project of Tyria2D that allows us to read the content files all at once

function contentLayout() {
  //main: grid
  //preview: hexa view
  //right: parsed files
  //left: strings, namespaces, fileRefs, localOffsets, externalOffsets, fileIndices, stringIndices, trackedReferences
}
